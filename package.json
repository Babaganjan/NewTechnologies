{
  "name": "newtechnologies",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sass": "sass styles/main.scss:public/css/styles.css --watch",
    "sass-build": "sass styles/main.scss:public/css/styles.css --style=compressed",
    "build": "npm run sass-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.93.1"
  }
}
