// import type { ProductConfig } from '@/shared/types/products.types';
//
// import { PRODUCT_CATALOG } from '../catalog';
//
// export const getProductById = (id: number): ProductConfig | undefined => {
//   for (const category of Object.values(PRODUCT_CATALOG)) {
//     const product = Object.values(category).find((p) => p.id === id);
//
//     if (product) return product;
//   }
//
//   return undefined;
// };
